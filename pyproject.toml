[project]
name = "verirag"
version = "1.0.0"
description = "A self-correcting document intelligence agent"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"

[tool.ruff]
line-length = 88
target-version = "py311"
